spring:
  profiles:
    include:
      - oauth.yml
      - jwt.yml
      - mail.yml
      - db-dev.yml
  h2:
    console:
      enabled: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/test
    username: root
    password: 
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
