spring:
  config:
    import:
      - jwt.yml
      - oauth.yml
      - mail.yml

  h2:
    console:
      enabled: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mentoringdb
    username: root
    password: zerobase
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
